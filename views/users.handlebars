<div class="container">
    <div class="row">
        <div class="col-8 offset-2 align-items-center">
            <h3>Users</h3>
            <ul id="userlist" class="list-group">
            {{#each user}}
                {{>user/user-block}}
            {{/each}}
            </ul>
            <a href="/">← Back To Home</a>
        </div>
    </div>
</div>